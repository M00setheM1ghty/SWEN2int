<Window x:Class="intermediateSWEN2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:intermediateSWEN2"
        xmlns:views="clr-namespace:intermediateSWEN2.Views" xmlns:viewmodels="clr-namespace:intermediateSWEN2.Viewmodels" d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1200">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Headline -->
            <RowDefinition Height="Auto"/>
            <!-- Menu -->
            <RowDefinition Height="Auto"/>
            <!-- Search Bar -->
            <RowDefinition Height="*"/>
            <!-- Main Content -->
        </Grid.RowDefinitions>

        <!-- Headline -->
        <TextBlock Grid.Row="0" Text="Tour Planner"
                   FontSize="24" FontWeight="Bold"
                   Margin="10" HorizontalAlignment="Center"/>

        <!-- Menu -->
        <Menu Grid.Row="1">
            <MenuItem Header="File"/>
            <MenuItem Header="Edit"/>
            <MenuItem Header="Options"/>
        </Menu>

        <!-- Search Bar Placeholder -->
        <TextBlock Grid.Row="2" Text="Search bar"
                   Margin="10" FontStyle="Italic" Foreground="Gray"/>

        <!-- Main Content Area -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="5*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Column="0" Grid.RowSpan="2" BorderBrush="Gray" BorderThickness="1" Margin="5">
                <views:TourView DataContext="{Binding TourViewModel}" />
            </Border>

            <Border Grid.Column="1" Grid.Row="0" BorderBrush="Gray" BorderThickness="1" Margin="5">
                <views:TourLogView DataContext="{Binding TourLogViewModel}" />
            </Border>

            <Border Grid.Column="1" Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Margin="5">
                <views:TourViewDetail DataContext="{Binding TourViewModel}" />
            </Border>
        </Grid>
    </Grid>


</Window>
