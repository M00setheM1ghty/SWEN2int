<Window x:Class="intermediateSWEN2.Popups.ModifiyTourLog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:intermediateSWEN2.Popups"
        mc:Ignorable="d"
        Title="ModifiyTourLog" Height="400" Width="400" WindowStartupLocation="CenterOwner">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Date:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <DatePicker SelectedDate="{Binding LogDate}" Grid.Row="0" Grid.Column="1"/>

        <TextBlock Text="Comment:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Text="{Binding LogComment}" Grid.Row="1" Grid.Column="1"/>

        <TextBlock Text="Difficulty:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox SelectedValue="{Binding LogDifficulty}" SelectedValuePath="Content" Grid.Row="2" Grid.Column="1">
            <ComboBoxItem Content="Easy"/>
            <ComboBoxItem Content="Medium"/>
            <ComboBoxItem Content="Hard"/>
        </ComboBox>

        <TextBlock Text="Total Distance:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Text="{Binding LogTotalDistance}" Grid.Row="3" Grid.Column="1"/>

        <TextBlock Text="Total Time (hh:mm:ss):" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Text="{Binding LogTotalTime}" Grid.Row="4" Grid.Column="1"/>

        <TextBlock Text="Rating:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox SelectedValue="{Binding LogRating}" SelectedValuePath="Content" Grid.Row="5" Grid.Column="1">
            <ComboBoxItem Content="0" />
            <ComboBoxItem Content="1" />
            <ComboBoxItem Content="2" />
            <ComboBoxItem Content="3" />
            <ComboBoxItem Content="4" />
            <ComboBoxItem Content="5" />
        </ComboBox>

        <TextBlock Text="{Binding Error}" Foreground="Red" Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,10,0,0"/>

        <StackPanel Orientation="Horizontal" Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="80" Margin="0,0,10,0" IsDefault="True" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
